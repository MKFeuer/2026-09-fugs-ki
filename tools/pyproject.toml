[project]
name = "tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "geographiclib>=2.1",
    "mcp[cli]>=1.27.0",
]

[dependency-groups]
dev = [
    "pytest>=8",
]
