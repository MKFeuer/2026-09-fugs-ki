# agentv2

Vue + Bun prototype for a fast live chat and planning workspace.

## Run

```bash
cd agentv2
cp .env.example .env
npm install
npm run dev
```

Edit `.env`, set the provider and model values, then open the app. The UI auto-connects to the configured model.
